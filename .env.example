# OpenAI API Configuration
# Replace the values with your actual API keys and settings
# Copy this file to .env and fill in your values:
# cp .env.example .env

# Required: API Key for OpenAI
OPENAI_API_KEY=your_api_key_here

# Required: Model to use (e.g., gpt-3.5-turbo, gpt-4o, etc.)
OPENAI_MODEL=gpt-3.5-turbo

# Optional: Base URL for OpenAI API compatible endpoints
# If you're using a different provider that's compatible with OpenAI's API
# Uncomment and set this value, otherwise the default OpenAI endpoint will be used
# OPENAI_BASE_URL=https://api.openai.com/v1 